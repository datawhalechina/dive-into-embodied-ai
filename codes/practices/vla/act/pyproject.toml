[project]
name = "lerobot-act-example"
version = "0.1.0"
description = "LeRobot ACT training example on the simulated ALOHA Transfer Cube dataset"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "httpx[socks]>=0.28,<1",
    "lerobot[aloha,training]==0.6.1",
    "matplotlib>=3.10,<4",
]

[tool.uv]
package = false
