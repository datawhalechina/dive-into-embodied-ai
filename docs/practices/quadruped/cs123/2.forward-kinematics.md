---
sidebar_position: 2
title: 2. 正运动学
description: "从关节角到末端位姿：多连杆机械臂的正运动学推导与 MuJoCo 实现。"
displayed_sidebar: practicesCs123CourseSidebar
---

import Figure from '@site/src/components/Figure';
import DocTable from '@site/src/components/DocTable';

# 2. 正运动学


上一章我们只控一个关节。这一章把关节串起来——给出 n 个关节角，求末端执行器在世界坐标系下的 6-DoF 位姿。正运动学是逆运动学、步态规划、视觉-动作对齐的前提。

## 本章目标

- 能写出齐次变换矩阵 `T = [R p; 0 1]`
- 能从 URDF / MJCF 描述里读出每个关节的父坐标系与旋转轴
- 能用 Python + NumPy 实现一个 3-DoF 机械臂的 FK，并在 MuJoCo 里验证末端位置误差 < 1e-6
- 能画出雅可比矩阵（为第 3 章逆解做准备）

## 前置阅读

- 第 1 章 [执行器与 PD 控制](/docs/practices/quadruped/cs123/pid-control)
- 想更直观理解，可以点击 [**交互模式**](/cs123/fk-playground) 学习


## 2.1 机械臂的运动组成

在推导出正运动学公式之前，我们先铺垫一些概念，首先是机械臂的运动组成。机械臂的运动由两个关键元素组成：**关节**(Joint)和**连杆**(Link)。此外还有一个特殊的连杆叫**末端执行器**(End Effector)，它是机械臂的"手"，在拓扑上是最后一个连杆，但功能特殊，所以单独命名。

- **关节**(Joint)：连接相邻两个连杆，提供一个或多个**自由度**(DoF)。最常见的是**旋转关节**(revolute joint)，提供一个绕固定轴的转角 $\theta_i$；另一类是**移动关节**(prismatic joint)，提供一个沿固定轴的位移 $d_i$。一个 $n$-DoF 机械臂的状态由所有关节变量构成的向量 $q \in \mathbb{R}^n$ 完全决定。
- **连杆**(Link)：相邻两关节之间的刚体段，自身位姿仅由父关节决定，与子关节的状态无关。FK 计算中，连杆的作用是给出"父关节到子关节"的固定齐次变换，与几何外形（长度、惯量、碰撞体）无关。

此外，还有两个特殊的连杆：**末端执行器**(End Effector)和**基座**(Base)。末端执行器（简称末端）是机械臂的"手"，在拓扑上是最后一个连杆，但功能特殊，所以单独命名；基座是机械臂的根部，通常固定在地面或工作台上，也是一个特殊的连杆，虽然它没有父关节。

<Figure
  src={require('./figs/joint-link-ee.webp').default}
  caption="关节、连杆与末端执行器的串联关系。"
  width={900}
/>

通常我们关心末端的运动状态（位置、姿态、位姿），而末端的状态完全由所有关节的状态决定。**正运动学**(Forward Kinematics, FK)就是这样一个函数：输入是 $n$ 个关节变量 $q = (\theta_1, \ldots, \theta_n)$，输出是末端执行器相对于基座或世界坐标系的位姿 ${}^B T_E$，具体下文再展开说明。

## 2.2 坐标系

要表达"末端在哪、朝哪"，必须先有参照物——坐标系就是这个参照物。机械臂里常见的坐标系有四种，按"由全局到局部"的顺序排列：

- **世界坐标系**(world frame)：通常记为 `{W}`，是整个仿真或现实场景的全局参考系，地板、桌面、目标物体的位置都在这套坐标系里给。
- **基座坐标系**(base frame)：通常记为 `{B}`，是机械臂根部所在的坐标系。它要么与世界坐标系重合（机械臂固定在原点），要么与世界坐标系之间有一个**已知**的固定变换（机械臂装在移动底盘或工作台上）。
- **连杆坐标系**(link frame)：每个连杆都有一个，记为 `{L_i}`，通常**贴在该连杆的父关节上**。它会随着前序关节转动而在世界里移动，是 FK 链式乘法的中间产物。
- **末端坐标系**(end-effector frame)：记为 `{E}`，贴在末端执行器上，是我们真正关心的输出坐标系。

<Figure
  src={require('./figs/coordinate-frames.webp').default}
  caption="四种坐标系与 FK 的链式变换。"
  width={1100}
/>

正运动学的核心就是**把末端坐标系 `{E}` 中表示的几何量（典型如原点 $O_E$）搬到基座坐标系 `{B}` 或世界坐标系 `{W}` 中**，具体下文再展开说明。

## 2.3 位姿

有了坐标系，我们就可以描述末端执行器的状态了。末端的状态通常用**位置**(position)和**姿态**(orientation)来刻画，其中位置刻画末端原点的坐标，即表示末端"在哪"，姿态刻画末端坐标轴的朝向，即表示末端"朝哪"。把两者合并到同一个变换中，就得到了**位姿**(pose)，是刚体状态的完整描述。

位置通常用一个三维向量 $p_E$ 表示，如式 $\eqref{eq:position}$ 所示：

$$
\begin{equation}\label{eq:position}
p_E = [x_E, y_E, z_E]^\top
\end{equation}
$$

姿态描述末端坐标系相对参考坐标系的旋转状态，相比位置，表达方式更多样、也更复杂，常见有三种：

- **旋转矩阵**(Rotation Matrix)：记为 $R \in \mathbb{R}^{3\times 3}$，是满足 $R^\top R = I$ 与 $\det(R) = 1$ 的正交矩阵。它的三列对应末端坐标系 `{E}` 三个坐标轴在参考坐标系 `{B}` 下的单位向量，因此 $R$ 既给出旋转的代数表示，又能直接读出"末端朝哪"。优点是合成只需矩阵乘、与齐次变换天然兼容；缺点是用 9 个数表示 3 个自由度，参数冗余。
- **欧拉角**(Euler Angles)：用绕三个轴依次旋转的角度组合表示姿态，最常见的是 Roll（翻滚）、Pitch（俯仰）、Yaw（偏航）三个角。只需 3 个数、对人类直观；但当两个旋转轴对齐时会发生**万向节死锁**(gimbal lock)，瞬时丢失一个自由度，因此不适合做积分或插值。
- **四元数**(Quaternion)：记为 $q = (w, x, y, z)$ 且 $\|q\| = 1$，用 4 个实数表示旋转。表达不直观，但全空间无奇异、数值稳定、插值平滑，是仿真器（MuJoCo / Isaac）和控制器内部最常用的姿态表示。

把位置 $p$ 与姿态 $R$ 并入同一个矩阵，便得到本章后续反复出现的**齐次变换矩阵**(homogeneous transformation matrix)，如式 $\eqref{eq:pose_matrix}$ 所示：

$$
\begin{equation}\label{eq:pose_matrix}
{}^B T_E =
\begin{bmatrix}
R & p \\
0 & 1
\end{bmatrix}
\in SE(3)
\end{equation}
$$

其中 $p$ 给出位置，$R$ 给出姿态。该矩阵的几何意义是：把末端坐标系 `{E}` 中表示的点变换到参考坐标系 `{B}` 中，如[图 3](#fig-pose) 所示。


<Figure
  id="fig-pose"
  src={require('./figs/pose-position-orientation.webp').default}
  caption="位置、姿态与位姿的几何关系。"
  width={1100}
/>


**正运动学**(Forward Kinematics, FK)就是这样一个函数，如式 $\eqref{eq:fk_def}$ 所示：

$$
\begin{equation}\label{eq:fk_def}
\text{FK}: \theta = (\theta_1, \theta_2, \ldots, \theta_n) \;\longmapsto\; {}^B T_E \in SE(3)
\end{equation}
$$

其中 $\theta_i$ 是第 $i$ 个关节的旋转角度，${}^B T_E$ 是末端执行器相对于基座坐标系的位姿。正运动学的计算内容就是把每个关节的旋转叠加起来，得到末端的整体位姿。

## 2.4 齐次变换矩阵

本节我们详细展开一下齐次变换矩阵，即式 $\eqref{eq:fk_def}$ 里那个"把末端坐标系 `{E}` 中表示的点变换到参考坐标系 `{B}` 中"的过程。

上一节末尾要把相邻坐标系之间的变换写成代数。这一节用几段代码逐步演示：旋转做什么、平移做什么、合起来又是什么——读完会自然得到 §2.2 里那个 $T = \begin{bmatrix} R & p \\ 0 & 1 \end{bmatrix}$ 的形式。

**旋转：让向量绕原点转。** 二维平面里一个点 $p = (1, 0)$ 绕原点逆时针转 $\theta$ 度，直接用旋转矩阵 $R(\theta)$ 乘上去：

```python
import numpy as np

def rot2(theta):
    c, s = np.cos(theta), np.sin(theta)
    return np.array([[c, -s], [s, c]])

p = np.array([1.0, 0.0])
print(rot2(np.deg2rad(90))  @ p)   # [0., 1.]   转到 y 轴
print(rot2(np.deg2rad(180)) @ p)   # [-1., 0.]  转到 -x 轴
```

旋转只改变方向，不改变长度，原点也保持不动——单凭旋转没办法把 $(1, 0)$ 搬到 $(2, 1)$ 这种"原点都换了位置"的目标。

**平移：整体搬一段距离。** 平移在代数上就是加一个向量 $t$：

```python
t = np.array([2.0, 1.0])
print(p + t)   # [3., 1.]
```

特征反过来：只改变位置，不改变方向。两件事合起来，足以描述刚体在平面里能做的全部运动——绕一个点转 + 整体挪一段。

**组合：先旋转再平移。** 写成代数就是课本里常见的形式：

$$
{}^A\!p = {}^A_BR\,\,{}^B\!p + {}^A\!p_{B,\text{org}}
$$

也就是：坐标系 $\{B\}$ 里的点 ${}^B\!p$ 先用 ${}^A_BR$ 旋到 $\{A\}$ 的方向，再加上 $\{B\}$ 原点在 $\{A\}$ 里的位置 ${}^A\!p_{B,\text{org}}$。代码验证：

```python
R = rot2(np.deg2rad(90))    # B 相对 A 转了 90°
t = np.array([2.0, 1.0])    # B 原点在 A 里的位置
p_B = np.array([1.0, 0.0])  # 这个点在 B 里的坐标

p_A = R @ p_B + t
print(p_A)   # [2., 2.]
```

**问题。** 旋转是矩阵乘法，平移是向量加法，两件事拼不到同一个运算里。一旦串起来三个连杆，就会写成 $R_3(R_2(R_1 p + t_1) + t_2) + t_3$ 这种嵌套——能写，但对人和代码都不友好。

**齐次形式：把两件事压成一个矩阵乘。** 给点补一维，把二维点 $(x, y)$ 写成 $(x, y, 1)$；把 $R$ 和 $t$ 拼成一个 3×3 矩阵：

$$
\begin{bmatrix} x' \\ y' \\ 1 \end{bmatrix} =
\begin{bmatrix} R & t \\ 0 & 1 \end{bmatrix}
\begin{bmatrix} x \\ y \\ 1 \end{bmatrix}
$$

```python
def make_T(R, t):
    T = np.eye(3)
    T[:2, :2] = R
    T[:2,  2] = t
    return T

T = make_T(rot2(np.deg2rad(90)), np.array([2.0, 1.0]))
p_B_h = np.array([1.0, 0.0, 1.0])    # 末尾的 1 是齐次坐标
p_A_h = T @ p_B_h
print(p_A_h)                         # [2., 2., 1.]  和上面手算一致
```

3D 同样处理：点变成 $(x, y, z, 1)$，矩阵变成 4×4，这就是 §2.2 提前预告过的**齐次变换矩阵**：

$$
T = \begin{bmatrix} R & p \\ 0 & 1 \end{bmatrix} \in SE(3)
$$

它把"位置 $p$ + 姿态 $R$"合并成单一对象，运算上只剩矩阵乘。

**两个关键性质**：

- **可以一路乘下去**。$n$ 个关节串起来的末端位姿就是 $T_0^n = T_0^1 T_1^2 \cdots T_{n-1}^n$，这一乘法正是 FK 的全部计算内容。
- **可逆且解析可写**。$T^{-1} = \begin{bmatrix} R^\top & -R^\top p \\ 0 & 1 \end{bmatrix}$，把世界坐标系下的目标搬到末端坐标系时会反复用到。


## 2.5 运动链

把基座、各连杆、末端执行器按"父→子"顺序连成一棵树（机械臂是单链，腿 / 手是分叉），就得到**运动链**(kinematic chain)。运动链上每条边对应一个关节，每个关节贡献一个齐次变换；FK 就是沿着这条链把所有齐次变换乘起来，如式 $\eqref{eq:fk_chain}$ 所示：

$$
\begin{equation}\label{eq:fk_chain}
{}^B T_E(\theta) = T_0^1(\theta_1)\,T_1^2(\theta_2)\,\cdots\,T_{n-1}^n(\theta_n)
\end{equation}
$$

剩下的问题只有一个：**怎么写出 $T_{i-1}^i(\theta_i)$**？目前主流有两种约定，分别是 **DH 参数**和 **URDF / MJCF 父-子链式约定**。

### 2.5.1 DH 参数

经典教材（Craig 那本）用 **DH 参数**描述机械臂：每个连杆四个参数 $(a_{i-1}, \alpha_{i-1}, d_i, \theta_i)$，对应一个固定形式的 4×4 矩阵。它的好处是"参数极少、推公式快"；缺点是**不可避免要让连杆坐标系和机械结构错位**，初学者很容易把连杆长度和 $d_i$ 混淆，更尴尬的是 DH 表和 URDF 之间没有自动转换工具，只能逐个关节手算。

### 2.5.2 URDF / MJCF 父-子链

URDF / MJCF 走的是另一条路——**父-子链式约定**：

```
world → base → link1 → link2 → link3 → ... → end_effector
         joint1   joint2   joint3
```

每条 `<joint>` 定义子连杆相对父连杆的**固定偏置 + 轴向**:

```xml
<joint name="joint2" type="revolute" axis="0 0 1">
  <origin xyz="0.3 0 0" rpy="0 0 0"/>
  <parent link="link1"/>
  <child  link="link2"/>
</joint>
```

- **静止部分** `origin`（平移 + 欧拉角）在 URDF 加载时就确定下来，本质是一个常量 $T_\text{offset}$。
- **运动部分**：绕 `axis` 旋转 $\theta$ 的增量 $R_\text{joint}(\theta)$。

于是第 $i$ 个关节对应的齐次矩阵总是可以写成式 $\eqref{eq:joint_offset}$ 的形式：

$$
\begin{equation}\label{eq:joint_offset}
T_{i-1}^i(\theta_i) = T_\text{offset}\cdot T_\text{joint}(\theta_i)
\end{equation}
$$

本章后面的代码都按这个更贴近 URDF 的写法走。好处是**我们写的 FK 代码可以直接从 URDF / MJCF 里读参数**，不用再手动换算一遍 DH。两种约定的对照见[表 1](#tab-dh-urdf)。

<DocTable id="tab-dh-urdf" caption="DH 参数与 URDF / MJCF 父-子链式约定的对照">

| 维度 | DH 参数 | URDF / MJCF 父-子链 |
| --- | --- | --- |
| 每个关节的描述 | 四个参数 $(a, \alpha, d, \theta)$ | 一个 `<origin>` + 一个 `axis` + 一个 $\theta$ |
| 坐标系位置 | 由 DH 规则强制（常与机械结构错位） | 自由放置（一般贴在父关节） |
| 与仿真器衔接 | 需要手算转换 | 加载即用 |
| 适合场景 | 教材推导、解析式 IK | 仿真、真机、可视化 |

</DocTable>

## 2.6 平面 3-DoF 手推

作为热身，先考虑最干净的平面 3-DoF 臂——三个连杆长度 $L_1, L_2, L_3$，三个 $z$ 轴旋转关节 $\theta_1, \theta_2, \theta_3$。末端位置如式 $\eqref{eq:planar_fk}$ 所示：

$$
\begin{equation}\label{eq:planar_fk}
\begin{aligned}
x &= L_1\cos\theta_1 + L_2\cos(\theta_1+\theta_2) + L_3\cos(\theta_1+\theta_2+\theta_3)\\
y &= L_1\sin\theta_1 + L_2\sin(\theta_1+\theta_2) + L_3\sin(\theta_1+\theta_2+\theta_3)
\end{aligned}
\end{equation}
$$

如果把末端姿态也一起写出来，这个平面例子其实还有一个很干净的结果：

$$
\phi = \theta_1 + \theta_2 + \theta_3
$$

也就是说，这个 3-DoF 平面臂的完整 FK 输出可以理解成 $(x, y, \phi)$。下一章解析 IK 时，这个 $\phi$ 会作为"末端朝向"重新出现。

注意这里关节角是**逐级累加**的——这是下一章解析 IK 能那么干净收敛的关键。

把它写成齐次矩阵形式，方便后面推广到 3D：

$$
\begin{equation}\label{eq:planar_chain}
T_0^3 = R_z(\theta_1)\,\mathrm{Trans}_x(L_1)\,R_z(\theta_2)\,\mathrm{Trans}_x(L_2)\,R_z(\theta_3)\,\mathrm{Trans}_x(L_3)
\end{equation}
$$

把三个 $R_z$ 展开一算，就会发现末端 $(x, y)$ 正是 $T_0^3$ 的第 4 列前两行；这也是所有 URDF 风格 FK 写法的通用套路——**乘出来就直接读平移向量**。

## 2.7 NumPy 实现与 MuJoCo 校验

先定义两个最基础的齐次变换构造器，后面所有 FK 代码都靠它们拼积木：

```python
import numpy as np

def rot_z(theta):
    c, s = np.cos(theta), np.sin(theta)
    T = np.eye(4)
    T[:3, :3] = [[c, -s, 0],
                 [s,  c, 0],
                 [0,  0, 1]]
    return T

def trans(x, y, z):
    T = np.eye(4)
    T[:3, 3] = [x, y, z]
    return T
```

把式 $\eqref{eq:planar_chain}$ 的链式乘法直接照抄成代码，就是 3-DoF 平面臂的 FK：

```python
def fk_planar(thetas, L=(0.3, 0.25, 0.15)):
    t1, t2, t3 = thetas
    T = rot_z(t1) @ trans(L[0], 0, 0) \
        @ rot_z(t2) @ trans(L[1], 0, 0) \
        @ rot_z(t3) @ trans(L[2], 0, 0)
    return T     # 末端位置直接读 T[:3, 3]
```

然后用 MuJoCo 的 `mj_forward` 作为"标准答案"交叉校验——给 100 组随机关节角，比较末端位置的最大误差。

先写一份与 `fk_planar` 默认参数对齐的最小 MJCF，存成 `planar_3dof.xml`：

```xml title="planar_3dof.xml"
<mujoco model="planar_3dof">
  <option gravity="0 0 0"/>

  <worldbody>
    <body name="link1" pos="0 0 0">
      <joint name="j1" type="hinge" axis="0 0 1"/>
      <geom type="capsule" fromto="0 0 0 0.3 0 0" size="0.02"/>

      <body name="link2" pos="0.3 0 0">
        <joint name="j2" type="hinge" axis="0 0 1"/>
        <geom type="capsule" fromto="0 0 0 0.25 0 0" size="0.018"/>

        <body name="link3" pos="0.25 0 0">
          <joint name="j3" type="hinge" axis="0 0 1"/>
          <geom type="capsule" fromto="0 0 0 0.15 0 0" size="0.015"/>
          <site name="end_site" pos="0.15 0 0" size="0.01"/>
        </body>
      </body>
    </body>
  </worldbody>
</mujoco>
```

三个 `hinge` 关节都绕 $z$ 轴；每节连杆的 `pos` 就是父关节到子关节的固定偏置 $(L_i, 0, 0)$，对应 §2.5.2 父-子链式约定里那一段固定变换；`end_site` 标在第三节末端，正是 Python 代码里 `mj_name2id(..., 'end_site')` 要查询的 site。

```python
import mujoco

model = mujoco.MjModel.from_xml_path('planar_3dof.xml')
data  = mujoco.MjData(model)
end_id = mujoco.mj_name2id(model, mujoco.mjtObj.mjOBJ_SITE, 'end_site')

max_err = 0.0
for _ in range(100):
    q = np.random.uniform(-np.pi, np.pi, size=3)
    data.qpos[:3] = q
    mujoco.mj_forward(model, data)           # 让 MuJoCo 用 kinematic tree 算
    p_mj = data.site_xpos[end_id]            # 它算的末端位置

    p_ours = fk_planar(q)[:3, 3]             # 我们自己算的末端位置

    max_err = max(max_err, np.linalg.norm(p_mj - p_ours))

print(f'max |p_ours - p_mj| = {max_err:.2e}')
# 期望量级在 1e-14 ~ 1e-12;超过 1e-6 基本是参数/轴向写错了
```

FK 是后面 IK、雅可比、控制器的共同基石——差 0.5 mm，在第 3 章末端画圆时就是一圈肉眼可见的毛刺，所以这一步要对齐到 `1e-12` 量级再往下走。

## 2.8 雅可比矩阵

FK 把关节角映射到末端位姿。若只关心位置部分，可以写成 $p = f(\theta)$；对时间求导就得到**雅可比矩阵**，它描述"关节角怎么动，末端怎么动"，如式 $\eqref{eq:jacobian}$ 所示：

$$
\begin{equation}\label{eq:jacobian}
\dot p = \frac{\partial f}{\partial \theta}\,\dot\theta = J(\theta)\,\dot\theta
\end{equation}
$$

对平面 3-DoF 臂，$J$ 是 $2\times 3$（平面位置对 3 个关节求偏导）；3D 一般机械臂是 $6\times n$（前 3 行位置、后 3 行旋转，分别叫线速度雅可比和角速度雅可比）。

有两种常用算法：

1. **解析求导**：把 FK 关于每个 $\theta_i$ 求偏导，适合符号化推一次、运行时直接算——速度快，但和具体机器人强绑定。
2. **几何雅可比**：对第 $i$ 列，线速度部分是 $\omega_i \times (p_\text{end} - p_i)$，角速度部分就是 $\omega_i$（关节轴在世界坐标系下的方向）。**代码写起来通用**，也是下一章 IK 用的那一种。

雅可比是运动学和动力学之间的桥：它一出现，**速度、力、能量**就都能用同一套代数来写——所以第 3 章数值 IK、第 5 章脚端接触力、第 6 章 RL 奖励里都会反复见到它。

## 2.9 实验 1：工作空间散点图

工作空间(reachable workspace) 是末端**能到达的所有点**的集合。用 FK 做一次 Monte Carlo 就能把它画出来：

```python
import matplotlib.pyplot as plt

pts = []
for _ in range(20000):
    q = np.random.uniform(-np.pi, np.pi, size=3)
    pts.append(fk_planar(q)[:2, 3])
pts = np.array(pts)

plt.figure(figsize=(5, 5))
plt.scatter(pts[:, 0], pts[:, 1], s=1, alpha=0.3)
plt.axis('equal'); plt.title('3-DoF planar arm workspace')
plt.show()
```

我们会看到一个**甜甜圈**形状的区域——外圈是三节完全伸直能到的最远距离 $L_1 + L_2 + L_3$，内圈是连杆互相折叠能贴到的最近距离。这张图在第 3 章选 IK 目标时会派上用场：**目标点落在甜甜圈外**就是典型的"无解"，这时再好的 IK 算法也救不了我们。

## 2.10 实验 2：末端位置叠加可视化

MuJoCo 支持在 viewer 里叠加"调试几何"，非常适合把 FK 的中间结果可视化出来——比如末端位置、每一节连杆坐标轴、目标点。

```python
import mujoco.viewer

with mujoco.viewer.launch_passive(model, data) as v:
    while v.is_running():
        # 假设我们在 Python 里用自己的 FK 算末端位置
        p = fk_planar(data.qpos[:3])[:3, 3]

        # 清空旧的调试几何再画
        v.user_scn.ngeom = 0
        mujoco.mjv_initGeom(
            v.user_scn.geoms[0],
            type=mujoco.mjtGeom.mjGEOM_SPHERE,
            size=[0.02, 0, 0], pos=p, mat=np.eye(3).flatten(),
            rgba=[1, 0.3, 0.3, 1],
        )
        v.user_scn.ngeom = 1

        mujoco.mj_step(model, data)
        v.sync()
```

这一步看似"只是画个红球"，但它实际上是**把 Python 世界里的数学模型和 MuJoCo 世界里的物理模型对上**。如果红球不在机械臂末端上，不是 viewer 的锅——要么坐标系没对齐，要么链式乘法少了一环、多了一环。到第 3 章做 IK 时，**目标点**和**当前末端**都是用这种方式实时叠到场景里的。

## 小结

- **机械臂的组成**：关节(Joint) 定义自由度，连杆(Link) 是关节之间的刚体；末端执行器是最后一个连杆，它的状态由**位置 + 姿态**合成的**位姿**(pose) 描述。
- **FK 是什么**：FK 把关节角映射到末端位姿 ${}^B T_E = \text{FK}(\theta)$，位姿用坐标系来描述——常见的有世界、基座、连杆、末端四种，FK 的链式乘法本质就是在它们之间逐级变换。
- **运动链与齐次变换**：把基座、各连杆、末端按"父→子"串起来就得到运动链；每个关节对应一个"**固定偏置 + 轴向旋转**"的变换，齐次变换矩阵 $T = \begin{bmatrix} R & p \\ 0 & 1 \end{bmatrix}$ 把位置和姿态合在一起，可以一路乘下去得到末端位姿。
- **两种描述约定**：DH 参数（教材推导）和 URDF / MJCF 父-子链式约定（仿真 / 真机）是描述运动链的两条路；后者更贴近实际机器人描述，本章代码全部按这一约定写。
- **从 FK 到雅可比**：齐次矩阵描述末端"在哪、朝哪"，雅可比 $J(\theta) = \partial f / \partial \theta$ 描述"关节角怎么动，末端怎么动"——它是运动学和动力学之间的桥梁，也是下一章数值 IK 的核心工具。

## 思考

1. §2.5.2 提到 URDF 的 `<origin>` 是常量 $T_\text{offset}$、`axis` + $\theta$ 是变量 $T_\text{joint}(\theta_i)$。如果某个关节的 `<origin rpy="0.5 0 0">`（父连杆预先绕 x 轴转了 0.5 rad），这 0.5 rad 应该出现在 FK 链式乘法的哪一项？关节角取 $\theta_i = 0$ 时末端位置会和 `rpy="0 0 0"` 时不同吗？

2. 把 §2.6 平面 3-DoF 的第三个关节换成移动关节（沿 link2 末端 $x$ 轴平移 $d_3$），末端位置 $(x, y)$ 与朝向 $\phi$ 该怎么写？为什么这种情况下 $\phi$ 仍然只取决于前两个旋转关节？

3. §2.7 自写 FK 与 `mj_forward` 的误差通常在 `1e-12 ~ 1e-14` 量级。如果把 MJCF 里某个 `<body pos>` 写成 `0.299 0 0`（少 1 mm），误差会跳到什么量级？三个关节里这种"参数对不齐"出在哪一节最严重？

4. §2.9 用 Monte Carlo 采样画出工作空间。给定 $L_1, L_2, L_3$，能不能解析写出甜甜圈外圈和内圈的半径？什么条件下三连杆可以折叠到原点（内圈缩到 0）？

5. §2.8 几何雅可比第 $i$ 列依赖 $p_\text{end} - p_i$。如果机械臂处于"完全伸直"姿态（所有 $\theta_i = 0$），这个雅可比的行列式会接近零——这个现象叫什么名字、几何意义是什么、对下一章的数值 IK 会带来什么麻烦？

## 动手任务

§2.10 那个红球只是把"Python FK"和"MuJoCo FK"在一条平面 3-DoF 臂上对齐了一次。本章动手任务把同一招升级到 Pupper 真腿——三个关节不再都是 $R_z$，而是 HAA → HFE → KFE 这种**混合轴**结构（按 §2.5 的 URDF / MJCF 父-子链式约定）。FK chain 写错一节，红球立刻飞出脚面——这是 Lab 自带的"自动出错检测"。

<div align="center">

![Ch2 成果预览:Pupper 单腿 FK 关节扫频](figs/lab2_leg_teleop.gif)

*脚本扫频版：HAA / HFE / KFE 依次在各自限位区间里单独扫一遍，红球按你写的 `fk_leg(θ)` 实时叠在 foot site 上。这是作品集 GIF；Lab 还自带一组 tkinter 滑杆 teleop 模式（`uv run python lab_2_fk_teleop/starter.py --viewer`），可以手动拖三个关节实时验证 FK——这才是 §2.10 那个红球的真腿版。*

</div>

要做的四件事：

- [ ] 在 Ch1 的 `scene.xml` 上删掉 `<equality>` 整块，让 HAA / HFE / KFE 都能动
- [ ] 写 `fk_leg(θ)` 的四段齐次变换链；100 组随机关节角对 `mj_forward` 误差 $< 10^{-10}$ m
- [ ] tkinter 三滑杆 + `mujoco.viewer.launch_passive`，红球实时叠在 foot 上
- [ ] Monte Carlo 采 2 万点，画 Pupper 工作空间"葫芦"散点（§2.9 平面甜甜圈的 3D 版）

{/* TODO(cs123-labs-migration): 代码迁入本仓库 docs/practices/quadruped/cs123/labs/ 后,补充 GitHub tree URL */}
完整 starter / 测试 / 交付清单见 `labs/lab_2_fk_teleop/`(代码施工中,稍后补充)。

## 参考资料

- CS123 Lecture 3: [Forward Kinematics](https://github.com/cs123-stanford/cs123-stanford-2023/blob/main/docs/schedule/lectures/fall-23/lec-3.rst)
- 《机器人学导论》（Craig）第 3 章
